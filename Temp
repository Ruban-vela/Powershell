$servers = Get-Content "servers.txt"
$chunkSize = 500
$index = 1

for ($i = 0; $i -lt $servers.Count; $i += $chunkSize) {

    $chunk = $servers[$i..([math]::Min($i + $chunkSize - 1, $servers.Count - 1))]

    $chunk | Out-File "servers_part$index.txt"

    $index++
}
